import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      // Browser calls relative /api/*; Vite forwards to FastAPI.
      // Same relative paths work later when FastAPI serves the built SPA.
      '/api': {
        target: 'http://127.0.0.1:8765',
        changeOrigin: true,
      },
      '/ws': {
        target: 'ws://127.0.0.1:8765',
        changeOrigin: true,
        ws: true,
      },
    },
  },
})
