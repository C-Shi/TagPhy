---
description: TagPhy development stages — guide for writing Trello stories
alwaysApply: false
---

## Guiding principle

- The scan pipeline (metadata, vision, storage, orchestration) is **plain Python functions**. Vision tagging is a **direct Gemini API call** (`google-genai`) behind `ImageVision`, not an LlmAgent. Optional local ONNX is a **privacy pre-check** (NSFW) before Gemini — not a tagging engine and not a local VLM.
- ADK is used **only where an agent is genuinely needed**: judgment, tool selection, iteration, or natural-language interaction. That is Stage Seven (below), which also fulfills the ADK learning goal.
- Rule of thumb: **Need a model? Call a model. Need an agent? Only if something must decide what to do next.**

## Non-agent baseline (Stages One–Six) — complete

Product loop for learning SE + local ONNX is closed. No further non-agent stories for now. Polish, packaging, concurrency, and non-agent ML extras stay off the board until Stage Seven is done (or you reopen them).

| Stage                                        | Status                                   |
| -------------------------------------------- | ---------------------------------------- |
| One — Setup                                  | **Done**                                 |
| Two — Core pipeline (CLI, Gemini)            | **Done**                                 |
| Three — Multi-file / directory scan          | **Done** (no parallel workers)           |
| Four — SQLite catalog                        | **Done**                                 |
| Five — Web GUI (scan, library, tags, detail) | **Done** (polish not on board)           |
| Six — Settings + NSFW ONNX gate              | **Done** (PII / ID-doc gate **removed**) |

```text
metadata → (if privacy_pre_check) NSFW ONNX
         → clear: Gemini tag_image → store
         → blocked/fail: failure_log, skip file
```

## Stage Seven: Agent system (ADK) — **next / only remaining track**

Living design for the Catalog Assistant and specialists. Challenge it when implementation starts. Former `.cursor/rules/agent-planning.mdc` content lives here.

### Boundaries

- Keep metadata → vision → storage **deterministic**. Agents do **not** replace the scan loop.
- Agents own natural-language intent, iterative decisions, tool choice, and human clarification.
- Python functions own retrieval, scoring, embedding compare, DB writes, and file ops.
- Search / investigation tools are **read-only**.
- Tag replace, entity backfill, file moves, and archival need **explicit human confirmation**.
- Persistent knowledge stays in **SQLite**; chat transcript is not memory.

### Use cases

#### Multi-agent

1. **Catalog Concierge**
   - Coordinate the interactive system; route to specialists.
   - Do not directly mutate files or DB rows.

2. **Interactive Photo Finder** — first conversational specialist (look for photo based on DB saved info)
   - Natural-language memory of a photo.
   - Search descriptions, tags, metadata, dates, locations, tag DAG.
   - Show likely thumbnails; remember rejects and constraints.
   - Ask focused follow-ups; broaden/narrow until the user finds the photo.

3. **Entity Onboarding** — must-have
   - Descriptions of named subjects (e.g. Meowy, Jelly).
   - Candidate photos; user confirms reference images.
   - Persistent embedding profiles from confirmed refs.
   - Backfill only high-confidence entity tags; ambiguous → user.
   - Later scans can add both generic (`cat`) and entity (`Meowy`) tags.

4. **Duplicate Resolver** — optional
   - Investigator: hashes, embeddings, metadata, dimensions, previews.
   - Judge: recommend canonical copy + evidence.
   - Never auto-delete; approve + archive rejects for review.

_(Earlier “Catalog Assistant” one-liner — search, explain `Unknown`, merge tags, re-run vision, triage failure log — is covered by Concierge + Photo Finder + Reanalysis tools, not a separate product.)_

### Development sequence

1. Non-agent baseline finished ← **here now**.
2. SQLite catalog queries, description search, thumbnail retrieval, structured tool return values.
3. **Interactive Photo Finder** (first conversational specialist) + ADK basics (`FunctionTool`, session state, Runner, `adk web`; reintroduce `agents/` as needed).
4. Image embeddings, reference-image records, confidence thresholds, correction-driven profile recalibration.
5. **Entity Onboarding**; wire confirmed profiles into future scans.
6. **Catalog Concierge** once multiple specialists exist; route through it.
7. **Duplicate Resolver** after hash/embedding similarity and safe archive exist.

## Explicitly out of scope (for now)

Not Trello stories until you reopen them (agent-track items above are **in** scope via Stage Seven):

- Multi-thread / multi-process scan workers
- Desktop packaging (Mac/Windows, single entrypoint)
- Naming-collision / deep error-handling epics beyond what exists
- Local VLM tagging; generic “tag count cap” product stories
- PII / ID-document visual or OCR gates
- Stage Five UI polish follow-ups

## Status

**Stages One–Six (non-agent): complete for this cut.**  
**Next: Stage Seven — start at sequence step 2–3 (catalog tool APIs + Interactive Photo Finder / ADK).**
